diff --git a/app/config.py b/app/config.py
index 10007d1..2a34203 100644
--- a/app/config.py
+++ b/app/config.py
@@ -5,6 +5,6 @@ DATABASE_URL = os.getenv("DATABASE_URL") or (
     f"{os.getenv('DB_USER', 'postgres')}:"
     f"{os.getenv('DB_PASS', 'postgres')}@"
     f"{os.getenv('DB_HOST', 'localhost')}:"
-    f"{os.getenv('DB_PORT', '5432')}/"
+    f"{os.getenv('DATABASE_PORT', '3306')}/"
     f"{os.getenv('DB_NAME', 'devops_rating')}"
 )
diff --git a/app/crud.py b/app/crud.py
index 383dbcd..cbc31ed 100644
--- a/app/crud.py
+++ b/app/crud.py
@@ -10,7 +10,7 @@ from app.models import Tool
 
 def get_all_tools(db: Session) -> list:
     """Returns all tools from the database, ordered alphabetically."""
-    return db.query(Tool).order_by(Tool.name).all()
+    return db.query(Tool).order_by(Tool.category).all()
 
 
 def get_tool_by_id(db: Session, tool_id: str):
@@ -21,4 +21,4 @@ def get_tool_by_id(db: Session, tool_id: str):
     .filter(...)   → add a WHERE clause
     .first()       → return the first result or None
     """
-    return db.query(Tool).filter(Tool.id == tool_id).first()
+    return db.query(Tool).first()
diff --git a/app/main.py b/app/main.py
index d983d35..dd4bf4e 100644
--- a/app/main.py
+++ b/app/main.py
@@ -9,7 +9,6 @@ app = FastAPI(
     version="1.0.0",
 )
 
-app.include_router(health.router)
 app.include_router(tools.router, prefix="/tools")
 
 
diff --git a/app/routers/tools.py b/app/routers/tools.py
index 530b4f9..efc6dbb 100644
--- a/app/routers/tools.py
+++ b/app/routers/tools.py
@@ -1,4 +1,5 @@
 from fastapi import APIRouter, BackgroundTasks, Depends, HTTPException, status
+import time
 from sqlalchemy.orm import Session
 
 from app.database import get_db
@@ -24,8 +25,9 @@ def sync_stars(background_tasks: BackgroundTasks):
 
 @router.get("/", response_model=list[ToolResponse])
 def list_tools(db: Session = Depends(get_db)):
+    time.sleep(2)
     """
-    GET /tools — Returns all tools in the database.
+    GET /tools - Returns all tools in the database.
 
     response_model=list[ToolResponse] tells FastAPI to:
       - Filter the ORM objects through the ToolResponse schema
diff --git a/app/services.py b/app/services.py
index 63f8a12..c664fdc 100644
--- a/app/services.py
+++ b/app/services.py
@@ -74,7 +74,6 @@ def sync_all_tool_stars() -> None:
                 if stars is None:
                     continue
                 tool.github_stars = stars
-                tool.last_synced_at = datetime.now(timezone.utc)
         db.commit()
         logger.info("Star sync complete for %d tools", len(tools))
     finally:
