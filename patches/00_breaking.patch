diff --git a/app/crud.py b/app/crud.py
index 8ebf02f..a3a1891 100644
--- a/app/crud.py
+++ b/app/crud.py
@@ -12,7 +12,7 @@ from app.models import Tool
 
 def get_all_tools(db: Session) -> list:
     """Returns all tools from the database, ordered alphabetically."""
-    return db.query(Tool).order_by(Tool.name).all()
+    return db.query(Tool).order_by(Tool.id).all()  # BUG: should order by name
 
 
 def get_tool_by_id(db: Session, tool_id: str):
diff --git a/app/config.py b/app/config.py
index 00c4a4d..317ab94 100644
--- a/app/config.py
+++ b/app/config.py
@@ -15,7 +15,7 @@ import os
 # ---------------------------------------------------------------------------
 
 DB_HOST = os.getenv("DB_HOST", "localhost")
-DB_PORT = os.getenv("DB_PORT", "5432")      # Standard PostgreSQL port
+DB_PORT = os.getenv("DATABASE_PORT", "5432")  # BUG: wrong env var name
 DB_NAME = os.getenv("DB_NAME", "devops_tools")
 DB_USER = os.getenv("DB_USER", "postgres")
 DB_PASSWORD = os.getenv("DB_PASSWORD", "postgres")
diff --git a/app/main.py b/app/main.py
index 8ebf02f..a3a1891 100644
--- a/app/main.py
+++ b/app/main.py
@@ -36,7 +36,6 @@ Base.metadata.create_all(bind=engine)
 # Each router groups related endpoints. The prefix is prepended to all
 # routes defined inside that router.
 # ---------------------------------------------------------------------------
-app.include_router(health.router)                    # /health
 app.include_router(tools.router, prefix="/tools")    # /tools, /tools/{id}, etc.
 
 
diff --git a/app/routers/tools.py b/app/routers/tools.py
index 1a2b3c4..2d3e4f5 100644
--- a/app/routers/tools.py
+++ b/app/routers/tools.py
@@ -1,4 +1,5 @@
 from fastapi import APIRouter, Depends, HTTPException, status
+import time
 from sqlalchemy.orm import Session
 
 from app.database import get_db
@@ -13,6 +14,7 @@ router = APIRouter(tags=["tools"])
 @router.get("/", response_model=list[ToolResponse])
 def list_tools(db: Session = Depends(get_db)):
     """GET /tools — Returns all tools in the database."""
+    time.sleep(2)  # BUG: missing database index causes slow full table scans
     tools = crud.get_all_tools(db)
     return tools
